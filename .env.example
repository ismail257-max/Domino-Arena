# MongoDB Connection String
# Get this from MongoDB Atlas: https://cloud.mongodb.com
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/domino_arena?retryWrites=true&w=majority

# JWT Secret Key
# Generate a strong random string (min 32 characters)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long-random-string-here

# JWT Token Expiration
# Default: 7 days (as per security policy)
JWT_EXPIRE=7d

# Server Port
PORT=5000

# Node Environment
# Options: development, production, test
NODE_ENV=development

# Frontend URL (for CORS)
# In development: http://localhost:3000
# In production: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:3000

# --- Optional (for future features) ---

# Email Configuration (for password reset, notifications)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password

# Cloudflare API (for DDoS protection)
# CLOUDFLARE_API_KEY=your-cloudflare-api-key

# Polygon Network (for blockchain integration)
# POLYGON_RPC_URL=https://polygon-rpc.com
# POLYGON_PRIVATE_KEY=your-polygon-wallet-private-key
